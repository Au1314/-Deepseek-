# 糖尿病管理助手 — 功能扩展分析文档

## 一、项目现状概述

### 1.1 项目定位
糖尿病预治智能助手的前端 Web 应用，采用移动端优先的响应式设计，模拟 App 交互体验。

### 1.2 技术架构
| 层面 | 技术 |
|------|------|
| 前端 | 原生 HTML/CSS/JS + Tailwind CSS |
| UI 组件 | Swiper.js（轮播）、SweetAlert2（弹窗）、Font Awesome（图标） |
| 后端 AI | Dify 平台（多个工作流 API） |
| 数据库 | Express + SQLite（通过 Dify Text2SQL 间接操作） |
| 数据交互 | Fetch API + SSE 流式传输 |

### 1.3 现有功能模块（23项已实现）
- 用户注册/登录 + 记住密码
- 首页轮播图 + 医生列表 + 科普文章 + 糖尿病类型知识
- 生活方案定制 + 打卡功能 + 打卡记录查看 + AI打卡分析
- 个性化健康资讯 + 资讯详情 + 资讯收藏管理
- AI控糖助手聊天 + 医师在线咨询
- 糖尿病风险评估 + 风险结果展示
- 个人信息查看/修改 + 通知设置 + AI管理平台

---

## 二、数据库表使用现状分析

### 2.1 表使用情况统计

| 状态 | 数据表 | 说明 |
|------|--------|------|
| ✅ 高频使用 | `users` | 登录注册核心 |
| ✅ 高频使用 | `user_risk_info` | 个性化功能数据基础 |
| ✅ 高频使用 | `life_plans` | 方案定制 |
| ✅ 高频使用 | `punch_in` | 打卡系统 |
| ✅ 高频使用 | `life_advice` | 资讯收藏 |
| ✅ 高频使用 | `doctor_information` | 医师咨询 |
| ⚠️ 部分使用 | `articles` | 有展示，无收藏功能 |
| ⚠️ 部分使用 | `diabetes_types` | 有展示，无用户关联 |
| ❌ 未使用 | `medication_records` | 服药记录表已建，无前端页面 |
| ❌ 未使用 | `check_records` | 检查记录表已建，无前端页面 |
| ❌ 未使用 | `check_in_records` | 体征签到表已建，无前端页面 |
| ❌ 未使用 | `article_collections` | 文章收藏表已建，前端未实现 |
| ❌ 未使用 | `slides` | 轮播图表已建，使用静态图片 |

### 2.2 关键发现
1. **3张表完全未使用**：`medication_records`、`check_records`、`check_in_records`
2. **2张表部分使用**：`articles`（无收藏）、`slides`（静态图片）
3. **表结构与功能不匹配**：已有表结构但缺少对应的前端页面

---

## 三、可扩展功能分析

### 3.1 短期可实现功能（基于已有数据表）

#### 📌 功能1：服药提醒模块
**优先级**：高  
**难度**：中  
**依赖表**：`medication_records`

**功能描述**：
- 服药记录管理（添加/编辑/删除）
- 服药提醒定时通知
- 服药依从性统计
- 药物库存提醒

**数据库扩展建议**：
```sql
-- 新增字段
ALTER TABLE medication_records ADD COLUMN reminder_time TEXT;  -- 提醒时间
ALTER TABLE medication_records ADD COLUMN is_active INTEGER DEFAULT 1;  -- 是否启用提醒
ALTER TABLE medication_records ADD COLUMN stock_count INTEGER;  -- 药物库存数量
```

**前端页面**：
- 服药记录列表页
- 添加/编辑服药记录表单
- 服药提醒设置页

---

#### 📌 功能2：体征监测模块
**优先级**：高  
**难度**：中  
**依赖表**：`check_in_records`

**功能描述**：
- 血糖/血压/体重记录
- 体征数据趋势图表
- 异常值预警
- 数据导出

**数据库扩展建议**：
```sql
-- 新增字段
ALTER TABLE check_in_records ADD COLUMN unit TEXT;  -- 单位（mmol/L、kg等）
ALTER TABLE check_in_records ADD COLUMN notes TEXT;  -- 备注
ALTER TABLE check_in_records ADD COLUMN is_fasting INTEGER;  -- 是否空腹（血糖专用）
```

**前端页面**：
- 体征记录表单（支持快捷输入）
- 体征趋势图表页（使用 Chart.js/ECharts）
- 体征数据列表页

---

#### 📌 功能3：文章收藏功能
**优先级**：中  
**难度**：低  
**依赖表**：`article_collections`

**功能描述**：
- 文章详情页添加收藏按钮
- 已收藏文章列表
- 取消收藏

**实现方案**：
- 在 `main/article.html` 添加收藏按钮
- 调用 `fetchSQLWorkflow('在article_collections中添加一条数据...')`
- 在 `mine/mine.html` 添加"我的收藏"入口

---

#### 📌 功能4：搜索功能
**优先级**：中  
**难度**：低  
**依赖表**：`articles`、`doctor_information`、`diabetes_types`

**功能描述**：
- 文章搜索
- 医生搜索
- 糖尿病类型搜索

**实现方案**：
- 在首页添加搜索框
- 调用 `fetchSQLWorkflow('查询标题包含...的文章')`
- 搜索结果列表展示

---

#### 📌 功能5：帮助中心
**优先级**：低  
**难度**：低  
**依赖表**：无

**功能描述**：
- FAQ 常见问题
- 使用指南
- 联系客服
- 关于我们

**实现方案**：
- 静态页面或从数据库读取
- 在个人中心添加跳转入口

---

### 3.2 中期扩展功能

#### 📊 功能6：数据可视化仪表盘
**优先级**：高  
**难度**：中

**功能描述**：
- 血糖趋势图（折线图）
- 体重变化图（折线图）
- 打卡完成率（饼图/环形图）
- 用药依从性（柱状图）
- 周/月健康报告

**技术方案**：
- 使用 Chart.js 或 ECharts
- 数据来源：`punch_in`、`check_in_records`、`medication_records`

---

#### 📅 功能7：打卡日历视图
**优先级**：中  
**难度**：中

**功能描述**：
- 月历形式展示打卡记录
- 连续打卡天数统计
- 打卡热力图

**技术方案**：
- 自定义日历组件
- 数据来源：`punch_in`

---

#### 📝 功能8：健康报告生成
**优先级**：中  
**难度**：中高

**功能描述**：
- 定期生成健康总结报告
- 支持导出为图片/PDF
- 包含各项指标趋势分析

**技术方案**：
- 使用 html2canvas 生成图片
- 数据来源：所有用户数据表

---

#### 🔔 功能9：智能提醒系统
**优先级**：中  
**难度**：中

**功能描述**：
- 服药提醒
- 打卡提醒
- 血糖监测提醒
- 复诊提醒
- 自定义提醒时间

**技术方案**：
- 使用 Web Notifications API + Service Worker
- 本地存储提醒配置

---

#### 📱 功能10：PWA 改造
**优先级**：中  
**难度**：中

**功能描述**：
- 添加到桌面
- 离线访问
- 推送通知
- 更接近原生 App 体验

**技术方案**：
- 添加 manifest.json
- 实现 Service Worker
- 优化资源缓存策略

---

### 3.3 长期扩展功能

#### 🏥 功能11：医生预约系统
**优先级**：中  
**难度**：高

**功能描述**：
- 医生排班查看
- 在线预约挂号
- 预约提醒
- 问诊记录

**数据库扩展**：
```sql
-- 新增表
CREATE TABLE appointments (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER,
  doctor_id INTEGER,
  appointment_time TEXT,
  status TEXT,  -- 待就诊/已就诊/已取消
  notes TEXT
);
```

---

#### 👥 功能12：用户社区/论坛
**优先级**：低  
**难度**：高

**功能描述**：
- 用户发帖交流
- 经验分享
- 话题讨论
- 点赞评论

**数据库扩展**：
```sql
CREATE TABLE posts (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER,
  title TEXT,
  content TEXT,
  create_time TEXT,
  likes INTEGER DEFAULT 0
);

CREATE TABLE comments (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  post_id INTEGER,
  user_id INTEGER,
  content TEXT,
  create_time TEXT
);
```

---

#### ⌚ 功能13：智能穿戴设备对接
**优先级**：低  
**难度**：高

**功能描述**：
- 对接血糖仪
- 对接智能手环
- 自动同步数据
- 实时监测

**技术方案**：
- 设备 API 对接
- 蓝牙数据传输
- 数据格式标准化

---

#### 🤖 功能14：AI 智能分析升级
**优先级**：中  
**难度**：高

**功能描述**：
- 基于历史数据的个性化预测
- 并发症风险评估
- 用药方案智能推荐
- 饮食运动智能规划

**技术方案**：
- 扩展 Dify 工作流
- 增加数据分析模型

---

#### 🌐 功能15：多语言支持
**优先级**：低  
**难度**：中

**功能描述**：
- 中文/英文切换
- 国际化框架集成

**技术方案**：
- 使用 i18next 或自定义方案
- 语言包管理

---

## 四、功能优先级矩阵

| 优先级 | 功能 | 难度 | 价值 | 建议 |
|--------|------|------|------|------|
| P0 | 体征监测模块 | 中 | 高 | 立即开发 |
| P0 | 服药提醒模块 | 中 | 高 | 立即开发 |
| P1 | 文章收藏功能 | 低 | 中 | 快速实现 |
| P1 | 搜索功能 | 低 | 中 | 快速实现 |
| P1 | 数据可视化仪表盘 | 中 | 高 | 优先开发 |
| P2 | 帮助中心 | 低 | 低 | 有空时做 |
| P2 | 打卡日历视图 | 中 | 中 | 按计划开发 |
| P2 | 智能提醒系统 | 中 | 中 | 按计划开发 |
| P3 | 健康报告生成 | 中高 | 中 | 后续迭代 |
| P3 | PWA 改造 | 中 | 中 | 后续迭代 |
| P4 | 医生预约系统 | 高 | 中 | 长期规划 |
| P4 | AI 智能分析升级 | 高 | 高 | 长期规划 |
| P5 | 用户社区/论坛 | 高 | 低 | 长期规划 |
| P5 | 智能穿戴设备对接 | 高 | 中 | 长期规划 |
| P5 | 多语言支持 | 中 | 低 | 按需开发 |

---

## 五、技术实现建议

### 5.1 开发顺序建议
```
第一阶段（1-2周）：
  ├── 体征监测模块（check_in_records 表）
  ├── 服药提醒模块（medication_records 表）
  └── 文章收藏功能（article_collections 表）

第二阶段（2-3周）：
  ├── 搜索功能
  ├── 数据可视化仪表盘（Chart.js）
  └── 帮助中心

第三阶段（3-4周）：
  ├── 打卡日历视图
  ├── 智能提醒系统
  └── 健康报告生成

第四阶段（长期）：
  ├── PWA 改造
  ├── 医生预约系统
  └── AI 智能分析升级
```

### 5.2 技术选型建议
| 功能 | 推荐技术 | 备选方案 |
|------|----------|----------|
| 数据可视化 | Chart.js（轻量） | ECharts（功能丰富） |
| 日历组件 | 自定义实现 | FullCalendar |
| 提醒系统 | Web Notifications + Service Worker | 第三方推送服务 |
| PWA | 原生实现 | Workbox |
| 图表导出 | html2canvas | svg2pdf.js |

### 5.3 数据库扩展建议
1. **统一数据类型**：修复 `medication_records.user_id` 类型不一致问题（TEXT→INTEGER）
2. **添加索引**：为常用查询字段添加索引
3. **数据备份**：实现定期数据导出功能

---

## 六、风险与注意事项

### 6.1 技术风险
- **Text2SQL 安全性**：自然语言转 SQL 存在注入风险，建议增加输入校验
- **API 依赖**：完全依赖 Dify 平台，需考虑离线降级方案
- **数据一致性**：多表操作需保证事务一致性

### 6.2 用户体验风险
- **功能复杂度**：避免功能过多导致界面臃肿
- **性能优化**：大量数据图表需优化加载速度
- **兼容性**：确保不同设备浏览器兼容

### 6.3 数据安全风险
- **密码明文**：建议升级为哈希存储
- **敏感数据**：健康数据需加密存储
- **隐私保护**：符合相关法规要求

---

## 七、总结

### 7.1 核心发现
1. 项目已有完整的功能框架，但有 **3张数据表完全未使用**
2. 已建表但未实现的功能是最容易扩展的方向
3. 现有架构支持快速迭代开发

### 7.2 最高价值扩展
1. **体征监测模块**：补全核心健康管理功能
2. **服药提醒模块**：提升用药依从性
3. **数据可视化**：增强数据洞察力

### 7.3 预期收益
- **用户粘性**：更多功能 = 更高使用频率
- **数据价值**：更完整数据 = 更精准分析
- **产品竞争力**：更丰富功能 = 更强市场竞争力

---

*文档生成时间：2026年7月15日*  
*基于项目现状和数据表字典分析*
